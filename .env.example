# Copy to .env and fill in, then run ./scripts/init-letsencrypt.sh && docker compose up -d

# Domain that points (DNS A/AAAA record) at this server
MAX_DOMAIN=max.example.com

# Email for Let's Encrypt expiry notices
LETSENCRYPT_EMAIL=you@example.com
